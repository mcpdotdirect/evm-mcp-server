apiVersion: v1
kind: Service
metadata:
  name: evm-mcp-server
  namespace: evm-mcp-server
  labels:
    app.kubernetes.io/name: evm-mcp-server
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
      appProtocol: HTTP
  selector:
    app.kubernetes.io/name: evm-mcp-server
