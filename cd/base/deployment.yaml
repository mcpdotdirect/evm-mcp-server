apiVersion: apps/v1
kind: Deployment
metadata:
  name: evm-mcp-server
  namespace: evm-mcp-server
  labels:
    app.kubernetes.io/name: evm-mcp-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: evm-mcp-server
  template:
    metadata:
      labels:
        app.kubernetes.io/name: evm-mcp-server
    spec:
      containers:
        - name: evm-mcp-server
          image: evm-mcp-server
          ports:
            - containerPort: 3000
              name: http
              protocol: TCP
          envFrom:
            - configMapRef:
                name: evm-mcp-server
          resources:
            requests:
              memory: "128Mi"
            limits:
              memory: "512Mi"
